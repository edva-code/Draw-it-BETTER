namespace Draw.it.Server.Models.User;

public enum AchievementId
{
    ArtisticRookie,   // GamesPlayed >= 3
    QuickDraw,        // FastGuesses >= 5
    Centurion,        // TotalScore >= 100
    Master,           // GamesWon >= 5
    TheGrandmaster,   // GamesWon >= 25
    MindReader,       // CorrectGuesses >= 50
    ArtisticSoul      // GamesPlayed >= 25
}